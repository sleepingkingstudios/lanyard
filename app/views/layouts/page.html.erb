<!DOCTYPE html>
<html>
  <head>
    <title>Lanyard</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= stylesheet_link_tag 'librum-core/application', media: 'all' %>
    <%= javascript_importmap_tags %>

    <%= csp_meta_tag %>
  </head>

  <body>
    <%=
      render(
        Librum::Core::View::Layouts::Page.new(
          alerts:      flash.each.to_h,
          footer_text: 'Amare Et Sapere Vix Deo Conceditur',
          title:       'Lanyard',
          **(@result&.metadata&.fetch(:page, {}) || {})
        )
      ) do
    %>
      <%= yield %>
    <% end %>
  </body>
</html>
